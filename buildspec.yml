version: 0.2

phases:
  install:
    runtime-versions:
      java: 17
  build:
    commands:
      - echo Build started on `date`
      - chmod +x ./gradlew
      - ./gradlew build
artifacts:
  files:
    - 'build/libs/*.jar'
