version: 0.2

phases:
  build:
    commands:
      - chmod +x gradlew
      - ./gradlew bootJar

artifacts:
  files:
    - build/libs/*.jar
    - appspec.yml
    - deploy-scripts/**/*
  discard-paths: no