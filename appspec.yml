version: 0.0

os: linux

files:
  - source: /
    destination: /home/ec2-user/deploy

permissions:
  - object: /home/ec2-user/deploy
    owner: ec2-user
    group: ec2-user

hooks:
  ApplicationStart:
    - location: deploy-scripts/start.sh
      timeout: 60
      runas: ec2-user